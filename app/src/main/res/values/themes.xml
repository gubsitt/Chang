<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Chang" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/light_gray</item>  <!-- ✅ ใช้แทน colorBackground -->
    </style>

    <style name="Theme.Chang" parent="Base.Theme.Chang" />
</resources>
